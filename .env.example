# Docker Compose (PostgreSQL)
POSTGRES_DB=crm
POSTGRES_USER=crm
POSTGRES_PASSWORD=crm
POSTGRES_PORT=5432

# Next.js (папка app/) — на хосте подключайтесь к localhost
# DATABASE_URL=postgresql://crm:crm@localhost:5432/crm
